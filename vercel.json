{
  "buildCommand": "node ./node_modules/@angular/cli/bin/ng build --configuration production --base-href /",
  "outputDirectory": "dist/user-dashboard",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}